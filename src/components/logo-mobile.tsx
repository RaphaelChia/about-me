import Logo from "@/components/logo";

const LogoMobile = () => {
  return (
    <div className="font-black text-2xl uppercase font-sans h-[60px] pad-x-page flex items-center">
      <Logo />
    </div>
  );
};

export default LogoMobile;
